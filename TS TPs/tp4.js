function parser(toParse) {
    for (var _i = 0, toParse_1 = toParse; _i < toParse_1.length; _i++) {
        var element = toParse_1[_i];
        console.log(element);
    }
}
var arr = [2, 5, 8, 9, 4, 7];
parser(arr);
